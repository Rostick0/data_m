import React, { FC } from 'react';
import styles from './SegmentUsersInfo.module.scss';

interface SegmentUsersInfoProps {}

const SegmentUsersInfo: FC<SegmentUsersInfoProps> = () => (
  <div className={styles.SegmentUsersInfo}>
    SegmentUsersInfo Component
  </div>
);

export default SegmentUsersInfo;
