export interface Statistic {
    successfully?: number | undefined,
    total?: number | undefined
}

export interface Statistic_2 {
    shipped?: number | undefined,
    read?: number | undefined,
    received?: number | undefined
}